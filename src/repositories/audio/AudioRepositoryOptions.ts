export interface AudioRepositoryOptions {
  /**
   * Whether to save the audio file to the specified path.
   */
  saveFile?: boolean

  /**
   * The path to save the audio file to.
   */
  saveFileAudioPath?: string
}
